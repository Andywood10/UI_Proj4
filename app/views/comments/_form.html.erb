<%= form_for([@experiment, @experiment.comments.build]) do |f| %>
	<% f.error_messages %>

	<div class="field">
		<%= f.text_field :comment %>
	</div>
	<div class="actions">
		<%= f.submit "Add Comment" %>
	</div>
<% end %> 
